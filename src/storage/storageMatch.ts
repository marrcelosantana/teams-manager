import AsyncStorage from "@react-native-async-storage/async-storage";
import { PlayerDTO } from "@models/PlayerDTO";

export async function storageCreateMatch(
  userId: string,
  players: PlayerDTO[],
  teams_quantity: number,
  players_quantity: number
) {
  try {
  } catch (error) {
    throw error;
  }
}
